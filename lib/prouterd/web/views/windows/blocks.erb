<table class="data-table">
  <thead>
    <tr>
      <th>Block</th>
      <th>Process</th>
      <th>Interface</th>
      <th>Call</th>
      <th>Timeout</th>
      <th>Retry</th>
    </tr>
  </thead>
  <tbody>
    <% @blocks.each do |b| %>
      <tr class="data-table__row--clickable"
          data-open-window="process"
          data-open-resource="<%= b[:process] %>">
        <td><%= b[:name] %></td>
        <td><%= b[:process] %></td>
        <td><%= b[:interface_label] || "—" %></td>
        <td class="data-table__truncate"><%= b[:call_summary] || "—" %></td>
        <td><%= b[:timeout_ms] ? "#{b[:timeout_ms]} ms" : "—" %></td>
        <td><%= b[:retry_policy] || "—" %></td>
      </tr>
    <% end %>
    <% if @blocks.empty? %>
      <tr><td colspan="6" class="data-table__empty">No blocks defined.</td></tr>
    <% end %>
  </tbody>
</table>
