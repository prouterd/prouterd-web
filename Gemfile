source "https://rubygems.org"

gemspec

# During co-development, resolve the core gem from the sibling checkout.
# Drop or override this stanza when consuming a published `prouterd` gem.
gem "prouterd", path: "../prouterd"
